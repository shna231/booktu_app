<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alexna.booktu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".accountActivity.MySharedPreferences"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
    <activity android:name="IntroActivity"
        android:theme="@style/SplashTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
        <activity android:name=".reviewActivity.ShowReviewActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".reviewActivity.SelectBookActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".accountActivity.SignupActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".accountActivity.MyInfoActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".accountActivity.LoginActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".reviewActivity.WriteReviewActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".ComunityActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".DiscussionActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat">
        </activity>
    </application>

</manifest>